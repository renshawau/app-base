{
  "name": "app-base",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm --filter web dev",
    "typecheck": "pnpm -r typecheck",
    "deploy": "node scripts/prepare-deploy.mjs && pnpm --filter web exec wrangler deploy --cwd ../.."
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  }
}
